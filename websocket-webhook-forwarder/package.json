{
  "name": "websocket-webhook-forwarder",
  "version": "1.0.0",
  "description": "Listens to a WebSocket and forwards messages to a Cloudflare Worker webhook.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "ts-node src/index.ts",
    "dev": "ts-node src/index.ts"
  },
  "keywords": [
    "websocket",
    "webhook",
    "cloudflare",
    "forwarder"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.5.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "ws": "^8.17.1",
    "dotenv": "^16.4.5"
  }
}
